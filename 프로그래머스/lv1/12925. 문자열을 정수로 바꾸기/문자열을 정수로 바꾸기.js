function solution(s) {
    let answer =  parseInt(s)
    let arr = s.split('');
    let sign = '';
    
//     if(s.include(-)) sign = arr.shift()
    
    //     if(arr[0] == '-'){
    //         sign = arr.shift();
    //     }
    // answer = arr.map(el => el === '-'? sign = el: += el);
    
        
    // console.log(typeof sign)
    
    return  parseInt(s);
}